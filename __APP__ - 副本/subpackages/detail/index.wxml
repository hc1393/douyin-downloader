<import src="/subpackages/detail/template/goods-list-item3ca5da55.wxml"></import>
<std-navigation-bar background="{{navigationBarBg}}" fixed="{{true}}" title="{{navigationBarTitle}}" wx:if="{{!isDefaultNav}}"></std-navigation-bar>
<view class="w-full bg-transparent navigation-block {{takeoutType==='showmap'?'absolute left-0 top-0':''}}" style="height: {{systemTitleHeight}}px"></view>
<view class="w-100_p_ flex items-center left-0 right-0 z-11 justify-between h-72 pl-30 pr-26 pt-0 pb-0 bg-_h_fff8e8 overflow-hidden box-border" wx:if="{{subscribeMessageIsShow&&!(isShowMap&&!paymentOnBehalf&&!isFromPOSScanCode&&takeoutType!='takeoutOver')}}">
    <view class="subscribe-text-wrapper">
        <view class="text-24 color-_h_FF8F0F">订阅订单提醒，不错过制作进度</view>
    </view>
    <view class="flex items-center">
        <view bindtap="subscribeMessage" class="w-136 h-48 text-center rounded-30 line-height-48 text-24 bg-_h_FF8800 color-_h_ffffff mr-18">立即订阅</view>
        <text bindtap="closeSubscribeMessage" class="i-close color-_h_999999"></text>
    </view>
</view>
<image class="fixed top-0 w-full" mode="widthFix" src="{{__oss__.s(mpxExt,topBgImg,0)}}" wx:if="{{isLoad&&topBgImg}}"></image>
<view class="fixed top-0 left-0 w-full h-360 z--1" wx:if="{{isLoad&&takeoutType&&takeoutType!=='showmap'&&!topBgImg}}">
    <image class="w-full h-full" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2024/12/25/takeoutdetailbg.png',0)}}"></image>
</view>
<view class="w-full h-800" wx:if="{{isShowMap&&!paymentOnBehalf&&!isFromPOSScanCode&&takeoutType!='takeoutOver'}}">
    <map showLocation controls="{{controls}}" id="map" latitude="{{latitude}}" longitude="{{longitude}}" markers="{{markers}}" scale="15" style="width: 100%; height: 100%">
        <cover-view slot="callout">
            <cover-view class="flex flex-col items-center" markerId="0">
                <cover-view class="market-cover-wrapper">
                    <cover-view class="market-cover-title">{{marketTitle}}</cover-view>
                    <cover-view class="market-cover-content" wx:if="{{marketCharacter}}">{{marketCharacter}}<cover-view class="market-cover-distance" style="color: {{colorTheme}}">{{marketDistance}}</cover-view>
                    </cover-view>
                </cover-view>
            </cover-view>
        </cover-view>
    </map>
</view>
<view class="text-48 font-500 pl-24 relative ml-24 mt-10" wx:if="{{takeoutType&&takeoutType!=='showmap'}}">{{orderStateText||stateInfo.title}}</view>
<view class="binded-page" wx:if="{{isLoad&&isBindOther&&isFromPOSScanCode}}">
    <view class="binded-img">
        <image class="binded-image" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2022/08/04/kaipiaonodata.png',0)}}"></image>
    </view>
    <view class="binded-text">很抱歉，该笔订单已被其他用户绑定</view>
</view>
<view class="p-0 p-x-24 p-b-22 relative {{modeLarge=='large'?'main-box-old':''}}" style="{{subscribeMessageIsShow?'padding-bottom:120rpx;':''}}" wx:elif="{{isLoad&&(!isFromPOSScanCode||!isBindOther&&isFromPOSScanCode)}}">
    <view class="page-head" wx:if="{{isFromPOSScanCode&&(needBind||!isAuth)&&isLoad&&!isBindOther}}">
        <view>您当前未登录，可能无法获取订单积分信息</view>
        <button catchtap="autoMobile" class="page-head-btn" wx:if="{{!isAuth}}">立即登录</button>
        <view bindtap="invoicingDetailBindOrderUser" class="page-head-btn" wx:elif="{{needBind}}">立即绑定</view>
    </view>
    <view class="payment-on-behalf-success" wx:if="{{getPaidInfo&&getPaidInfo.showPayer}}">
        <image class="payment-on-behalf-success-avatar" lazyLoad="true" src="{{__oss__.s(mpxExt,getPaidInfo.payerImage||'https://images.qmai.cn/resource/20210824210816/2024/02/21/pin-avatar.png',0)}}"></image>
        <text>{{_i1}}</text>
    </view>
    <block wx:if="{{!paymentOnBehalf}}">
        <order-detail-top-tips transparent="{{!!topBgImg}}" words="到店取餐时，需刷校园一卡通支付" wx:if="{{showCampusDetailTips}}"></order-detail-top-tips>
        <view class="pin-wrapper" wx:if="{{isShowPinWrapper}}">
            <view class="pin-head-title">{{_i2}}</view>
            <view class="pin-head-content">
                <view class="pin-price-content">
                    <view>{{_i3}}</view>
                    <view class="pin-price-tip" style="color:{{colorTheme}}">{{currencySymbol}}</view>
                    <view class="pin-price-number" style="color:{{colorTheme}}">{{result.orderInfo.spellingSingleAmount||0}}</view>
                </view>
                <view class="pin-pay-content">
                    <view>{{_i4}}</view>
                    <image class="pin-pay-img" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2021/06/24/2bbe708137fe4f17.png',0)}}"></image>
                </view>
            </view>
        </view>
        <view class="state-wrap wrap relative" style="background: {{topBgImg?'transparent':'#FFF'}};{{takeoutType?'padding: 32rpx 24rpx;':''}}{{takeoutType==='showmap'&&carrierPhone?'margin-top:40rpx;':''}}{{takeoutType==='takeoutOver'?'padding-top:10rpx;':''}}" wx:elif="{{stateInfo}}">
            <view class="s-title text-center_i_" wx:if="{{result.dyOrderId&&dyOrderWaiting}}">{{_i7}}</view>
            <block wx:else>
                <view class="order-tip" wx:if="{{result.orderInfo.payAbnormalOrder==1}}">{{_i8}}</view>
                <block wx:if="{{!takeoutType}}">
                    <view class="{{'takefood-title '+(orderType!=5&&kindlyPasscodeText&&hasShowKindlyPasscodeSwiper?'justify-between_i_':'')+' '}}" wx:if="{{sortNumObj||payModel==2&&result.status==10}}">
                        <text class="s-title text-center_i_">{{orderStateText||stateInfo.title}}</text>
                        <view bindtap="showLuckyPoster" class="luncky-share-tips flex items-center" style="color:{{colorTheme}}" wx:if="{{orderType!=5&&kindlyPasscodeText&&hasShowKindlyPasscodeSwiper}}">
                            <view class="rounded-36rpx w-36rpx h-36rpx flex items-center justify-center" style="background-color:{{tools.hexToRgba(colorTheme,0.1)}};">
                                <text class="iconfont i-cup text-28rpx"></text>
                            </view>
                            <text class="text-24rpx ml-10rpx">晒今日幸运签</text>
                        </view>
                    </view>
                    <view class="s-title text-center_i_" wx:else>{{orderStateText||stateInfo.title}}</view>
                </block>
                <view class="w-full bg-white rounded-t-20 flex flex-col absolute top--104 left-0 box-border" wx:if="{{takeoutType&&takeoutType==='showmap'&&carrierPhone}}">
                    <view class="h-84 px-24 flex items-center justify-between">
                        <view class="flex items-center">
                            <view class="w-60 h-60 rounded-full overflow-hidden">
                                <image class="w-full h-full" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2025/01/03/qishouicon.png',0)}}"></image>
                            </view>
                            <view class="text-28 ml-16">骑手</view>
                        </view>
                        <view bindtap="callPhoneThirdDelivery">
                            <text class="i-tel text-24" style="color:{{colorTheme}}"></text>
                            <text class="ml-16 text-24" style="color:{{colorTheme}}">{{_i9}}</text>
                        </view>
                    </view>
                    <view class="h-20 flex relative items-center">
                        <view class="ml--10 bg-_h_F5F5F5 w-20 h-20 absolute left-0 top-0" style="border-radius: 50%"></view>
                        <view class="flex-1 hairline-round"></view>
                        <view class="mr--10 bg-_h_F5F5F5 w-20 h-20 absolute right-0 top-0" style="border-radius: 50%"></view>
                    </view>
                    <view class="h-20"></view>
                </view>
                <view class="text-36 font-500" wx:if="{{autoCloseOrderAt&&zdCancelTime&&takeoutType}}">{{_i10}}<text class="ml-20" style="color:{{colorTheme}}">{{zdCancelTime}}</text>
                </view>
                <view class="s-subtitle text-center_i_" wx:if="{{autoCloseOrderAt&&zdCancelTime&&!takeoutType}}">{{_i11+' '+zdCancelTime}}</view>
                <view class="subtitle-text" wx:elif="{{stateInfo.subtitle}}">{{stateInfo.subtitle}}</view>
                <view class="flex items-start {{takeoutType==='daijiedan'?'justify-center pb-18':'justify-between pb-48'}}" wx:if="{{takeoutType&&takeoutType!='takeoutOver'&&!(autoCloseOrderAt&&zdCancelTime)&&(sortNumObj||payModel==2&&result.status==10)}}">
                    <view class="flex-1 text-36 font-500" wx:if="{{takeoutType!=='daijiedan'}}">
                        <text wx:if="{{takeoutType==='showmap'}}">{{orderStateText||stateInfo.title}}，</text>
                        <view class="flex flex-col" wx:if="{{result.orderQueueInfo&&result.orderQueueInfo.countDown}}">
                            <view wx:if="{{!(hasCountDownOverFlag||orderQueueCountDown==0)}}">
                                <text class="iconfont i-dengdaizhongbeifen text-36" style="color: {{colorTheme}};"></text>
                                <text class="ml-8">{{_i12}}</text>
                                <view class="display-inline-block ml-6">
                                    <count-down bindcountDownOver="countDownOver" scene="style_3" second="{{orderQueueCountDown}}" timeStyle="color:{{hasCountDownOverFlag?'#f93a4a':colorTheme}};font-size:36rpx;background:#fff!important;" unitStyle="color:{{hasCountDownOverFlag?'#f93a4a':colorTheme}}!important;font-size:36rpx;"></count-down>
                                </view>
                            </view>
                            <text wx:if="{{hasCountDownOverFlag||orderQueueCountDown==0}}">{{_i13}}<text style="color: {{colorTheme}}">{{_i14}}</text>{{_i15}}</text>
                            <text class="text-24 color-_h_999 mt-16 font-400" wx:else>{{_i16}}</text>
                        </view>
                        <block wx:elif="{{(orderType==3&&deliveryTimeShow||orderType!=3)&&result.queueTime&&!isAfterOrderCompletion}}">{{result.queueTimeText}}<text style="color:{{colorTheme}}">{{result.queryMonthDayHourMinute}}</text>
                        </block>
                        <block wx:elif="{{orderType==3&&result.orderInfo.state==80&&result.sendTime}}">{{_i17}}<text style="color:{{colorTheme}};">{{result.sendHourMinute}}</text>{{_i18}}</block>
                        <order-progress deliveryTimeShow="{{deliveryTimeShow}}" id="orderProgress" isOnlyText="{{true}}" orderQueueInfo="{{result.orderQueueInfo}}" orderType="{{orderType}}" queueShowContent="{{queueShowContent}}" ref="__subweSaveRef__" sendHourMinute="{{result.sendHourMinute}}" sendTime="{{result.sendTime}}" wx:elif="{{!isFromPOSScanCode&&showOrderQueueInfo&&!result.queueTime}}"></order-progress>
                    </view>
                    <view bindtap="showLuckyPoster" class="flex items-center flex-shrink-0 ml-30" style="color:{{colorTheme}}" wx:if="{{orderType!=5&&kindlyPasscodeText&&hasShowKindlyPasscodeSwiper}}">
                        <view class="rounded-36rpx w-36rpx h-36rpx flex items-center justify-center" style="background-color:{{tools.hexToRgba(colorTheme,0.1)}};">
                            <text class="iconfont i-cup text-28rpx"></text>
                        </view>
                        <text class="text-24rpx ml-10rpx">晒今日幸运签</text>
                    </view>
                </view>
                <view bindtap="showLuckyPoster" class="flex items-center justify-center mt-22 mb-18" style="color:{{colorTheme}}" wx:if="{{takeoutType&&takeoutType==='takeoutOver'&&orderType!=5&&kindlyPasscodeText&&hasShowKindlyPasscodeSwiper}}">
                    <view class="rounded-36rpx w-36rpx h-36rpx flex items-center justify-center" style="background-color:{{tools.hexToRgba(colorTheme,0.1)}};">
                        <text class="iconfont i-cup text-28rpx"></text>
                    </view>
                    <text class="text-24rpx ml-10rpx">晒今日幸运签</text>
                </view>
                <block wx:if="{{orderType!=5}}">
                    <kindly-passcode kindlyWords="{{kindlyPasscodeText}}" orderNum="{{sortNumObj.value}}" takeoutType="{{takeoutType}}" wx:if="{{kindlyPasscodeText}}"></kindly-passcode>
                    <block wx:elif="{{!takeoutType}}">
                        <view class="takefood-detail-num-wrapper" wx:if="{{(sortNumObj.value||payModel==2&&result.status==10)&&orderInfoStatus!=70}}">
                            <block wx:if="{{sortNumObj}}">
                                <block wx:if="{{result.tableName&&result.tableName!='无桌号'}}">
                                    <view class="w-full">
                                        <order-numbers orderFont="{{sortNumObj.name}}" orderNum="{{sortNumObj.value}}" tableFont="{{_i19}}" tableNum="{{result.tableName}}"></order-numbers>
                                    </view>
                                    <view class="take-meals-tips" wx:if="{{result.orderInfo.state!=90&&isTakefood}}">{{orderDetailDesign.takeMealTipsTableNumber}}</view>
                                </block>
                                <block wx:else>
                                    <image class="meals-wait" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/12/28/131c510f050531fa.gif',0)}}" wx:if="{{sortNumObj.value==0}}"></image>
                                    <view class="meals-num" wx:else>{{sortNumObj.value}}</view>
                                    <view class="meals-text">{{sortNumObj.name}}</view>
                                    <view class="take-meals-tips" wx:if="{{result.orderRegionNotic&&(result.orderType==1||result.orderType==2)&&(orderInfoStatus==30||orderInfoStatus==40||orderInfoStatus==50)}}">{{result.orderRegionNotic}}</view>
                                    <view class="take-meals-tips" wx:elif="{{result.orderInfo.state!=90&&isTakefood}}">{{orderDetailDesign.takeMealTipsWithoutTableNumber}}</view>
                                </block>
                            </block>
                            <block wx:elif="{{result.tableName}}">
                                <view class="meals-num">{{result.tableName}}</view>
                                <view class="meals-text">{{_i20}}</view>
                                <view class="take-meals-tips" wx:if="{{result.orderInfo.state!=90&&isTakefood}}">{{orderDetailDesign.takeMealTipsWithoutTableNumber}}</view>
                            </block>
                        </view>
                    </block>
                </block>
                <order-progress-list list="{{progressList}}" takeoutType="{{takeoutType}}" wx:if="{{progressList&&progressList.length&&!isShowSelfCupOrderCode&&takeoutType&&takeoutType!='takeoutOver'}}"></order-progress-list>
                <view class="code-info" wx:if="{{isShowRule&&( !isFromPOSScanCode||isFromPOSScanCode&&!(subscribeDialogShow&&!isBindOther) )}}">
                    <view class="meal-code-canvas-block">
                        <canvas canvasId="mealcode" class="meal-code-canvas" id="mealcode" type="2d"></canvas>
                    </view>
                    <image class="meal-code-canvas" lazyLoad="true" src="{{__oss__.s(mpxExt,mealcodeImg,0)}}"></image>
                    <view class="code-desc" wx:if="{{result.orderType!=3&&orderInfoStatus==40}}">{{_i21}}</view>
                    <view class="code-desc" wx:else>{{_i22}}</view>
                </view>
                <view class="code-info" wx:if="{{isShowSelfCupOrderCode}}">
                    <image class="code-info__img" lazyLoad="true" src="{{__oss__.s(mpxExt,selfCupCodeImg,0)}}"></image>
                    <view class="self-cup-canvas-box">
                        <canvas canvasId="selfCupOrderCode" class="self-cup-canvas" id="selfCupOrderCode" type="2d"></canvas>
                    </view>
                    <view class="self-cup-tip">{{_i23}}</view>
                </view>
                <view class="order-tip" style="font-size: 26rpx; font-weight: 500; color: #999" wx:if="{{isMultiUnPay}}">请等待订单提交人支付订单</view>
                <view class="s-btn-wrap flex items-center justify-center relative">
                    <view class="flex-1" wx:if="{{stateInfo.btnsMore&&stateInfo.btnsMore.length}}"></view>
                    <view catchtap="showMoreButton" class="order-btn-more" data-orderno="{{orderNo}}" wx:if="{{stateInfo.btnsMore&&stateInfo.btnsMore.length}}">
                        <image class="order-btn-more-img" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/08/04/68bee9e8a13454b3.png',0)}}"></image>
                        <view class="order-btn-more-wrap absolute" style="{{showOrderMorebtn==orderNo?'':'display:none;'}}">
                            <image class="ico-b-m-icon absolute" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/08/04/07ae257357723f16.png',0)}}"></image>
                            <view catchtap="stateBtnOperation" class="ico-b-m-i-item" data-item="{{btn}}" style="{{stateInfo.btnsMore.length-1==btnIdx?'border:none':''}}" wx:for="{{stateInfo.btnsMore}}" wx:for-index="btnIdx" wx:for-item="btn" wx:key="btn">{{btn.name}}</view>
                        </view>
                    </view>
                    <view class="flex-1"></view>
                    <block wx:for="{{stateInfo.btns}}" wx:key="stateInfoBtn">
                        <view class="zm-button-wrape" wx:if="{{item.type=='GETPAID'}}">
                            <zm-button btnType="GETPAID" data-share-type="GETPAID" openType="share" type="{{stateInfo.btnStrong===item.type?'':'default'}}">{{item.name}}</zm-button>
                        </view>
                        <view class="zm-button-wrape" wx:elif="{{item.type=='share'}}">
                            <zm-button data-share-type="bill" openType="share" type="default">{{item.name}}</zm-button>
                        </view>
                        <view class="zm-button-wrape" wx:elif="{{item.type=='ttLkIm'}}">
                            <zm-button binderror="customerServiceHrefError" bindlifeim="customerServiceHrefSuccess" openType="lifeIm" type="default">{{item.name}}</zm-button>
                        </view>
                        <view class="zm-button-wrape {{item.disabledStyle?'disabled-style':''}}" wx:else>
                            <zm-button bindbtntap="stateBtnOperation" btnBgImg="{{item.bgImg}}" data-item="{{item}}" disabled="{{item.disabled}}" type="{{stateInfo.btnStrong===item.type?'':'default'}}">{{item.bgImg?'':item.name}}</zm-button>
                        </view>
                    </block>
                    <block wx:if="{{stateInfoFinish&&result.status==10}}">
                        <block wx:if="{{result.payModel!=1&&!result.is_open_forhere_pay||result.payModel==1}}">
                            <view class="zm-button-wrape" wx:if="{{result.status==10&&!orderStatus&&result.orderType!=30}}">
                                <zm-button bindbtntap="showPayModal" disabled="{{payBtnDisabled}}">{{_i24}}</zm-button>
                            </view>
                            <view class="zm-button-wrape" wx:else>
                                <zm-button bindbtntap="payOrder" disabled="{{payBtnDisabled}}">{{_i25}}</zm-button>
                            </view>
                        </block>
                    </block>
                    <view class="flex-1"></view>
                </view>
                <view class="flex items-center justify-center text-28 mt-24" wx:if="{{(sortNumObj.value||payModel==2&&result.status==10)&&orderInfoStatus!=70&&sortNumObj&&takeoutType}}">
                    <view class="text-_h_999">{{sortNumObj.name}}</view>
                    <image class="w-68 h-72 ml-8" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/12/28/131c510f050531fa.gif',0)}}" wx:if="{{sortNumObj.value==0}}"></image>
                    <view class="font-500 ml-8" wx:else>{{sortNumObj.value}}</view>
                </view>
                <view bindtap="getOrder" class="lock-tips" wx:if="{{lockTips}}">{{lockTips}}<image class="lock-tips-icon" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/10/14/69908b4cba5ad8b4.png',0)}}"></image>
                </view>
                <view bindtap="toCouponList" class="flex items-center justify-center text-28 mt-24 color-_h_999" wx:if="{{result.orderType==8&&orderInfoStatus==50}}">
                    <view>优惠券已发放至您的账户</view>
                    <view class="ml-16" style="color:{{colorTheme}}">去查看</view>
                    <view class="ml-8 text-24 i-chaidanjiantou-you" style="color:{{colorTheme}}"></view>
                </view>
            </block>
        </view>
        <view bindtap="handleShowPopupSecurity" class="px-24 flex items-center justify-between box-border relative bg-_h_eee rounded-b-24" data-responeinfo="{{result.overTimePayInfoRespone}}" wx:if="{{result.overTimePayInfoRespone&&result.overTimePayInfoRespone.title}}">
            <view class="h-44 absolute top--22 left-0 right-0 z--1 bg-_h_eee"></view>
            <view class="flex items-center py-20">
                <view class="text-22 border-1 border-solid rounded-6 px-8 flex items-center justify-center flex-shrink-0" style="color:{{colorTheme}};border-color:{{colorTheme}}">{{result.overTimePayInfoRespone.shareTitle}}</view>
                <view class="text-24 ml-12">{{result.overTimePayInfoRespone.title}}</view>
            </view>
            <view class="text-24 ml-12 flex-shrink-0" style="color: {{colorTheme}};">优惠权益<text class="iconfont i-jiantou-you"></text>
            </view>
        </view>
    </block>
    <view class="info-wrap wrap" wx:if="{{isFromPOSScanCode&&(isShowTakeFoodProgress||isShowTakeOutProgress)}}">
        <view class="takeFood-progress" wx:if="{{isShowTakeFoodProgress}}">
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2022/08/04/已下单.png',0)}}" wx:if="{{orderInfoStatus==20}}"></image>
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2022/08/04/制作中.png',0)}}" wx:elif="{{orderInfoStatus==30}}"></image>
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2022/08/04/待取餐状态.png',0)}}" wx:elif="{{orderInfoStatus==40}}"></image>
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2022/08/04/制作完成状态.png',0)}}" wx:elif="{{orderInfoStatus==50}}"></image>
            <view class="progress-group">
                <view class="progress-item {{orderInfoStatus==20?'active':''}}">已下单</view>
                <view class="progress-item progress-two {{orderInfoStatus==30?'active':''}}">制作中</view>
                <view class="progress-item progress-three {{orderInfoStatus==40?'active':''}}">待取餐</view>
                <view class="progress-item {{orderInfoStatus==50?'active':''}}">已完成</view>
            </view>
        </view>
        <view class="takeOut-progress" wx:if="{{isShowTakeOutProgress}}">
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/12/18/c84916042edefe1d.png',0)}}" wx:if="{{result.orderInfo.state==30}}"></image>
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/12/18/3a6f7c4622920d9a.png',0)}}" wx:if="{{result.orderInfo.state==70}}"></image>
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/12/18/8dbb0d8052eba668.png',0)}}" wx:if="{{result.orderInfo.state==80}}"></image>
            <image class="progress-icon" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2020/12/18/5f5ce4e794fc364e.png',0)}}" wx:if="{{result.orderInfo.state==90}}"></image>
            <view class="progress-group">
                <view class="progress-item {{result.orderInfo.state==30?'active':''}}">待接单</view>
                <view class="progress-item progress-two {{result.orderInfo.state==70?'active':''}}">待配送</view>
                <view class="progress-item progress-three {{result.orderInfo.state==80?'active':''}}">配送中</view>
                <view class="progress-item {{result.orderInfo.state==90?'active':''}}">已送达</view>
            </view>
        </view>
        <view class="line-note" wx:if="{{(orderType==3&&deliveryTimeShow||orderType!=3)&&result.queueTime&&!isAfterOrderCompletion}}">{{result.queueTimeText}}<text style="color:{{colorTheme}};font-weight:bold;">{{result.queueTime}}</text>
            <block wx:if="{{orderType==1||orderType==2}}">{{_i26}}</block>
        </view>
        <order-progress deliveryTimeShow="{{deliveryTimeShow}}" id="orderProgress" orderQueueInfo="{{result.orderQueueInfo}}" orderType="{{orderType}}" queueShowContent="{{queueShowContent}}" ref="__subweSaveRef__" sendTime="{{result.sendTime}}" wx:elif="{{isShowOrderQueueInfo}}"></order-progress>
    </view>
    <view class="info-wrap wrap" style="padding-bottom: 32rpx" wx:if="{{progressList&&progressList.length&&!isShowSelfCupOrderCode&&!takeoutType}}">
        <view class="blind-box-progress-tip" style="color:{{colorTheme}};background:{{tools.hexToRgba(colorTheme,'0.1')}}" wx:if="{{result.orderInfo.state==40&&isHasBlindBoxGoods}}">商品制作完成后开盲盒，请耐心等待惊喜</view>
        <order-progress-list list="{{progressList}}"></order-progress-list>
        <view class="meal-countdown" wx:if="{{result.orderQueueInfo&&result.orderQueueInfo.countDown}}">
            <block wx:if="{{!(hasCountDownOverFlag||orderQueueCountDown==0)}}">
                <view class="meal-countdown-title" style="color: {{colorTheme}};">
                    <text class="iconfont i-dengdaizhongbeifen"></text>{{_i27}}</view>
                <view class="meal-countdown-main">
                    <count-down bindcountDownOver="countDownOver" scene="style_3" second="{{orderQueueCountDown}}" timeStyle="color:{{hasCountDownOverFlag?'#f93a4a':colorTheme}};font-size:64rpx;background:#fff!important;" unitStyle="color:{{hasCountDownOverFlag?'#f93a4a':colorTheme}}!important;"></count-down>
                </view>
            </block>
            <view class="meal-countdown-remarks">
                <block wx:if="{{hasCountDownOverFlag||orderQueueCountDown==0}}">{{_i28}}<text style="color: #f93a4a">{{_i29}}</text>{{_i30}}</block>
                <block wx:else>{{_i31}}</block>
            </view>
        </view>
        <view class="line-note" wx:elif="{{(orderType==3&&deliveryTimeShow||orderType!=3)&&result.queueTime&&!isAfterOrderCompletion}}">{{result.queueTimeText}}<text style="color:{{colorTheme}};font-weight:bold;">{{result.queueTime}}</text>
            <block wx:if="{{orderType==1||orderType==2}}">{{_i32}}</block>
        </view>
        <view class="line-note" wx:elif="{{orderType==3&&result.orderInfo.state==80&&result.sendTime}}">
            <text>{{_i33}}<text style="color:{{colorTheme}};font-weight:bold;">{{result.sendTime}}</text>{{_i34+_i35}}</text>
        </view>
        <order-progress deliveryTimeShow="{{deliveryTimeShow}}" id="orderProgress" orderQueueInfo="{{result.orderQueueInfo}}" orderType="{{orderType}}" queueShowContent="{{queueShowContent}}" ref="__subweSaveRef__" sendTime="{{result.sendTime}}" wx:elif="{{!isFromPOSScanCode&&showOrderQueueInfo&&!result.queueTime}}"></order-progress>
    </view>
    <view class="info-wrap wrap" wx:if="{{spellGroupInfo&&spellGroupInfo.teamStatus==0&&!paymentOnBehalf}}">
        <view class="group-info-wrapper">
            <view class="group-team-status-text">再邀请<text style="color: #f84036">{{spellGroupInfo.remainderTeamMembeLimit}}人</text>，立即成团</view>
            <view class="group-team-user">
                <view class="group-team-user-item" wx:for="{{spellGroupInfo.teamMemberList}}" wx:key="index">
                    <block wx:if="{{!item.isDefault}}">
                        <view class="group-team-user-master" wx:if="{{item.memberType==1}}">团长</view>
                        <image class="group-team-user-avatar {{item.memberType==1?'leader':''}}" lazyLoad="true" src="{{__oss__.s(mpxExt,item.userLogo||'https://images.qmai.cn/s1001195/images/2021/04/07/453ae66bf1cc283f.png',0)}}"></image>
                    </block>
                    <view class="group-team-detault" wx:else>{{item.defaultText}}</view>
                </view>
            </view>
            <button class="group-team-btn" openType="share">邀请好友参团</button>
        </view>
    </view>
    <view bindtap="goGroupDetail" class="group-success-wrapper" wx:if="{{spellGroupInfo&&spellGroupInfo.teamStatus==1&&!paymentOnBehalf}}">
        <view class="group-success-text">拼团成功</view>
        <view class="group-success-user-wrapper">
            <view class="group-success-avatar-item" wx:for="{{spellGroupInfo.teamMemberList}}" wx:key="index">
                <image class="group-success-avatar" lazyLoad="true" src="{{__oss__.s(mpxExt,item.userLogo,0)}}" style="z-index: {{spellGroupInfo.teamMemberList.length-index}}"></image>
                <view class="group-team-user-default" style="z-index: {{spellGroupInfo.teamMemberList.length-index}}" wx:if="{{item.defaultText}}">{{item.defaultText}}</view>
            </view>
        </view>
        <view class="group-success-right-icon baking i-chaidanjiantou-you"></view>
    </view>
    <view class="wx-contact-qr" wx:if="{{wxContactInfo&&wxContactInfo.isShow}}">
        <image showMenuByLongpress class="wx-contact-qr-bg" lazyLoad="true" src="{{__oss__.s(mpxExt,wxContactInfo.workWechatBg,0)}}"></image>
        <image showMenuByLongpress class="wx-contact-qr-source" lazyLoad="true" src="{{__oss__.s(mpxExt,wxContactInfo.workWechatSource,0)}}"></image>
    </view>
    <swiper autoplay bindchange="orderBannerSwiperChange" class="order-banner" wx:if="{{!(wxContactInfo&&wxContactInfo.isShow)&&orderBanner.length&&!paymentOnBehalf}}">
        <swiper-item class="order-banner-item" wx:for="{{orderBanner}}" wx:key="id">
            <image showMenuByLongpress bindtap="orderAdvertImgBindTap" class="order-banner-image" data-item="{{item}}" lazyLoad="true" src="{{__oss__.s(mpxExt,item.landingPageImg,0)}}"></image>
        </swiper-item>
    </swiper>
    <view class="payment-on-behalf-user-wrapper" wx:if="{{paymentOnBehalf}}">
        <view class="payment-on-behalf-user-avatar">
            <image class="payment-on-behalf-user-image" lazyLoad="true" src="{{__oss__.s(mpxExt,getPaidInfo.orderImage,0)}}" wx:if="{{getPaidInfo&&getPaidInfo.orderImage}}"></image>
            <image class="payment-on-behalf-user-image" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2024/02/21/pin-avatar.png',0)}}" wx:else></image>
        </view>
        <view class="payment-on-behalf-user-name">
            <text wx:if="{{getPaidInfo&&getPaidInfo.orderName}}">{{getPaidInfo.orderName}}</text>
            <text wx:else>下单人</text>
        </view>
        <view class="payment-on-behalf-user-title">
            <text wx:if="{{orderInfoStatus==10&&!(getPaidInfo&&!getPaidInfo.underSingle)}}">我发起了订单代付</text>
            <text wx:if="{{orderInfoStatus==10&&getPaidInfo&&!getPaidInfo.underSingle}}">hi，你的好友邀请你买单！</text>
            <text wx:if="{{orderInfoStatus==60}}">很遗憾，我的订单已取消</text>
            <text wx:if="{{orderInfoStatus!=10&&orderInfoStatus!=60}}">我们的友谊地久天长！</text>
        </view>
        <view class="payment-on-behalf-user-line"></view>
        <view class="payment-on-behalf-user-amount">
            <text wx:if="{{orderInfoStatus==10}}">{{currencySymbol}}{{result.orderInfo.actualAmount}}</text>
            <text wx:if="{{orderInfoStatus==60}}">订单已取消</text>
            <text wx:if="{{orderInfoStatus!=10&&orderInfoStatus!=60}}">支付成功</text>
        </view>
        <view class="payment-on-behalf-user-payment-time">
            <text wx:if="{{autoCloseOrderAt&&zdCancelTime}}">{{_i36+': '+zdCancelTime}}</text>
            <text wx:if="{{orderInfoStatus!=10&&orderInfoStatus!=60}}">订单支付成功，去通知好友</text>
        </view>
        <button class="payment-on-behalf-user-share" data-share-type="GETPAID" openType="share" style="background-color:{{colorTheme}}" wx:if="{{orderInfoStatus==10&&!(getPaidInfo&&!getPaidInfo.underSingle)}}">发给好友</button>
        <button bindtap="payOrder" class="payment-on-behalf-user-share" style="background-color:{{colorTheme}}" wx:if="{{orderInfoStatus==10&&getPaidInfo&&!getPaidInfo.underSingle}}">为好友买单</button>
        <button bindtap="goHomePage" class="payment-on-behalf-user-share" style="background-color:{{colorTheme}}" wx:if="{{orderInfoStatus==60&&getPaidInfo&&!getPaidInfo.underSingle}}">我也要找人买单</button>
        <button class="payment-on-behalf-user-share" data-share-type="NOTIFY" openType="share" style="background-color:{{colorTheme}}" wx:if="{{orderInfoStatus!=10&&orderInfoStatus!=60&&getPaidInfo&&!getPaidInfo.underSingle}}">通知好友</button>
    </view>
    <cash-back-order-item info="{{result.activityGiftDepositInfo}}" wx:if="{{showCashbackCom}}"></cash-back-order-item>
    <pay-gift-module consumeBenefitCustomSetting="{{consumeBenefitCustomSetting}}" isShowBtn="{{true}}" orderNo="{{orderNo}}" payGift="{{payGift}}" sellerConfig="{{activityDiyConfigBySellerMap[14]}}" wx:if="{{payGift}}"></pay-gift-module>
    <order-reward bindtoExchange="pointExchange" discountAccountTime="{{result.discountAccountTime}}" done="{{orderInfoStatus==50}}" exp="{{extraGainRight.experienceNum}}" expName="{{extraGainRight.experienceName}}" integral="{{extraGainRight.integral}}" integralName="{{pointName}}" orderDetailConfig="{{orderDetail}}" point="{{extraGainRight.starNum}}" pointComingBenefitInfo="{{pointComingBenefitInfo}}" pointName="{{extraGainRight.starName}}" starComingBenefitInfo="{{starComingBenefitInfo}}" wx:if="{{extraGainRight&&(extraGainRight.integral||extraGainRight.starNum||extraGainRight.experienceNum)}}"></order-reward>
    <view class="wrap" wx:if="{{orderInfoStatus==20&&result.orderInfo.state==80&&result.isSplitDelivery&&result.isSplitDelivery==1}}">
        <split-delivery-list id="splitDeliveryList"></split-delivery-list>
    </view>
    <view bindtap="startSurveyBanner" class="survey-banner-wrap" wx:if="{{orderDetailPopBanner}}">
        <image class="survey-banner-image" lazyLoad="true" src="{{__oss__.s(mpxExt,orderDetailPopBanner,0)}}"></image>
    </view>
    <block wx:if="{{result}}">
        <view class="pin-goods-serial-number flex items-start" wx:if="{{mySelfCrowdSerialNo}}">
            <view class="flex items-center">
                <text class="iconfont i-md-information-circle-outline serial-number-icon"></text>
                <text wx:if="{{result.showSplicingOrderPeople==3}}">{{isAuth?'请按手机尾号【'+phoneLastFour+'】取餐':'请按编号【'+mySelfCrowdSerialNo+'】取餐'}}</text>
                <text wx:elif="{{result.showSplicingOrderPeople==2}}">{{'请按编号【'+mySelfCrowdSerialNo+'】取餐'}}</text>
                <text wx:else>{{isAuth?'请按手机号【'+phoneDesensitization+'】取餐':'请按编号【'+mySelfCrowdSerialNo+'】取餐'}}</text>
            </view>
        </view>
        <view class="wrap {{mySelfCrowdSerialNo?'crowd-serial-no-wrap':''}}">
            <view class="info-title info-title-store line-bot" wx:if="{{result.shopInfo}}">
                <view class="info-title-left {{shopNameShopType==1?'line-clamp-2':'truncate'}}">
                    <text>{{result.shopInfo.name}}</text>
                </view>
                <view class="info-title-right">
                    <view bindtap="showBottomFrame" class="info-title-icon-wrapper">
                        <view class="iconfont i-dianhua" style="color: {{colorTheme}}"></view>
                    </view>
                    <view bindtap="gotoStore" class="info-title-icon-wrapper" wx:if="{{!isYongHeDaWangHK}}">
                        <view class="iconfont i-daohang" style="color: {{colorTheme}}"></view>
                    </view>
                </view>
            </view>
            <block wx:if="{{result.orderSubType!=2}}">
                <view wx:for="{{revealGoodsList}}" wx:key="index">
                    <view class="space-between store-info-new" wx:if="{{item.name&&!paymentOnBehalf&&isMultiOrder}}">
                        <view class="store-info-l">
                            <image class="store-img circle" lazyLoad="true" src="{{__oss__.s(mpxExt,item.avatar||'https://images.qmai.cn/resource/20210824210816/2023/12/05/avatar-default.png',0)}}"></image>
                            <text class="store-name">{{item.name}}</text>
                        </view>
                    </view>
                    <view class="goods-group">
                        <template is="goods-list-item" data="{{currencySymbol:currencySymbol,isGiftDeposit:goodsItem.isGiftDeposit,isFullSend:goodsItem.isFullSend,displayAreaName:goodsItem.displayAreaName,displayPocketName:goodsItem.displayPocketName,districtSort:goodsItem.districtSort,pocketSort:goodsItem.pocketSort,image:goodsItem.image,isGift:goodsItem.isGift,isRequired:goodsItem.isRequired,groupPurchase:goodsItem.groupPurchase,goodsType:goodsItem.goodsType,isMultiSet:goodsItem.isMultiSet,goodsDisabled:goodsItem.goodsDisabled,species:goodsItem.species,name:goodsItem.name,paidCardUnitDesc:goodsItem.paidCardUnitDesc,crowdSerialNo:goodsItem.crowdSerialNo,offlineIsReturn:goodsItem.offlineIsReturn,chargeGoodsText:goodsItem.chargeGoodsText,combinedList:goodsItem.combinedList,discountList:goodsItem.discountList,queueStatusText:goodsItem.queueStatusText,refundStatusText:goodsItem.refundStatusText,unit_type:goodsItem.unit_type,num:goodsItem.num,unit:goodsItem.unit,isWeighItems:goodsItem.isWeighItems,weighItemsNum:goodsItem.weighItemsNum,itemUnit:goodsItem.itemUnit,goodsAddStatusText:goodsItem.goodsAddStatusText,goodsAddStatusClass:goodsItem.goodsAddStatusClass,goodsPoints:goodsItem.goodsPoints,amount:goodsItem.amount,originAmount:goodsItem.originAmount,isFullGive:goodsItem.isFullGive,setMealGoods:goodsItem.setMealGoods,isGive:goodsItem.isGive,isPay:result.isOrderSettlement,colorTheme:colorTheme,pointName:pointName,orderType:result.orderType,energyText:goodsItem.energyText,activityDiyConfigBySellerMap:activityDiyConfigBySellerMap,mpxExt:mpxExt}}" wx:for="{{item.goodsList}}" wx:for-item="goodsItem" wx:key="index"></template>
                    </view>
                </view>
                <view class="custom-lunch-box" wx:if="{{specialPackPata}}">
                    <view class="lunch-box-bg">
                        <image class="lunch-box-img" lazyLoad="true" mode="aspectFill" src="{{__oss__.s(mpxExt,specialPackPata.goodsPicture||'https://images.qmai.cn/s1000084/images/2020/12/08/980498c9e68d97b0.png',0)}}"></image>
                    </view>
                    <view class="lunch-box-content">
                        <view class="custom-name-note">
                            <text class="custom-name">{{specialPackPata.diyName}}</text>
                            <text class="custom-note" wx:if="{{specialPackPata.extRemark}}">{{specialPackPata.extRemark}}</text>
                        </view>
                        <view class="custom-lunch-box-price" style="color: {{colorTheme}};">{{currencySymbol+specialPackPata.diyLunchBoxFeeTotal}}</view>
                    </view>
                </view>
            </block>
            <pin-goods activityDiyConfigBySellerMap="{{activityDiyConfigBySellerMap}}" crowdType="{{result.crowdType}}" customPinRuleText="{{result.customPinRuleText}}" goods="{{revealGoodsList}}" orderType="{{result.orderType}}" preferentialSharingRules="{{result.preferentialSharingRules}}" splicingOrderPeople="{{result.showSplicingOrderPeople}}" wx:else></pin-goods>
            <view catchtap="switchShowMore" class="more flex items-center justify-center" wx:if="{{goodsCount>2||goodsList&&goodsList.length>2}}">
                <text>{{_i37}}</text>
                <text>({{allGoodsCount||''}})</text>
                <text class="iconfont i-jiantou-xia {{isShowMore?'up':''}}"></text>
            </view>
            <view class="bill-list" wx:if="{{billList&&billList.length}}">
                <block wx:for="{{billList}}" wx:for-item="bill" wx:key="index">
                    <view bindtap="__invoke" class="bill-list-item" data-eventconfigs="{{ tap:[ ['handleClick','__mpx_event__',bill.event] ] }}">
                        <view class="bill-type">
                            <view class="bill-type-top">
                                <image class="image" lazyLoad="true" src="{{__oss__.s(mpxExt,bill.icon,0)}}" wx:if="{{bill.icon}}"></image>
                                <text class="jian" style="background: {{colorTheme}}" wx:elif="{{bill.tag}}">{{bill.tag}}</text>
                                <text>{{bill.desc}}</text>
                                <view catchtap="__invoke" class="bill-tip-icon iconfont i-md-information-circle-outline" data-eventconfigs="{{ tap:[ ['handleClick','__mpx_event__',bill.tipEvent] ] }}" wx:if="{{bill.packagingInfo}}"></view>
                            </view>
                            <view class="bill-type-bottom" wx:if="{{bill.cardIconUrl&&bill.cardName}}">
                                <image class="image" lazyLoad="true" src="{{__oss__.s(mpxExt,bill.cardIconUrl,0)}}" wx:if="{{bill.cardIconUrl}}"></image>
                                <text>{{bill.cardName}}</text>
                            </view>
                        </view>
                        <view class="bill-box" style="color:{{bill.colorFont}}">
                            <view class="amount" wx:if="{{bill.amountText}}">
                                <view class="amount-font">
                                    <text class="{{bill.subDescClass==='line-through'?'line-through':''}}" wx:if="{{bill.subDesc}}">{{bill.subDesc}}</text>
                                    <text>{{bill.amountText}}</text>
                                </view>
                                <view class="{{bill.tagFontClass==='bill-tag'?'bill-tag':''}}" wx:if="{{bill.tagFont}}">{{bill.tagFont}}</view>
                            </view>
                            <view class="more-text {{bill.moreType==='button'?'button2':''}}" style="color:{{bill.colorFont}}; border-color:{{bill.colorFont}}" wx:elif="{{bill.moreText}}">{{bill.moreText}}</view>
                            <view class="disable-text" wx:elif="{{bill.disableText}}">{{bill.disableText}}</view>
                            <view class="disable-text" wx:if="{{bill.pointNum}}">(<text style="color: {{colorTheme}};">{{bill.pointNum}}</text>{{pointName}})</view>
                            <image class="arrow-icon" lazyLoad="true" mode="widthFix" src="{{__oss__.s(mpxExt,'http://seller.qmai.cn/uploads/images/wxapp/arrow.png',0)}}" wx:if="{{bill.hasMore}}"></image>
                        </view>
                    </view>
                    <view class="bill-activity-title" wx:if="{{bill.activityTitle}}">{{bill.activityTitle}}</view>
                </block>
            </view>
            <view class="flex items-center justify-end h-104 relative mt-24 text-28" wx:if="{{result.isDisplayTheTotalAmountBeforePayment&&!paymentOnBehalf}}">
                <view class="absolute top-0 left-0 right-0 line-bot"></view>
                <view class="flex items-baseline">
                    <block wx:if="{{result.orderInfo.discountAmount}}">
                        <text class="lh-1">{{_i38}}</text>
                        <text class="text-_h_f93a4a lh-1">{{currencySymbol+result.orderInfo.discountAmount}}</text>
                    </block>
                    <text class="ml-20 lh-1">{{_i39}}</text>
                    <block wx:if="{{result.orderInfo.goodsPoints}}">
                        <text class="font-500 text-40 text-24 lh-1">{{result.orderInfo.goodsPoints}}</text>
                        <text class="lh-1">{{pointName}} +</text>
                    </block>
                    <text class="font-500 text-24 lh-1">{{currencySymbol}}</text>
                    <text class="font-500 text-40 lh-1">{{result.orderInfo.actualAmount}}</text>
                </view>
            </view>
        </view>
    </block>
    <view bindtap="linkRightCardsOrder" class="rights-cards-info-wrapper" data-order-no="{{result.recommendPaidBenefitCardDto.orderNo}}" wx:if="{{result.orderType!=13&&result.recommendPaidBenefitCardDto}}">
        <image class="before" src="{{__oss__.s(mpxExt,result.recommendPaidBenefitCardDto.paidCardImg,0)}}"></image>
        <view class="center">
            <text class="center-font-main">{{result.recommendPaidBenefitCardDto.paidCardName}}</text>
            <text class="center-font-support">{{currencySymbol+result.recommendPaidBenefitCardDto.memberPaidCardPrice.recommendBuyCardPrice}}</text>
        </view>
        <view class="after">查看详情</view>
    </view>
    <view class="info-wrap wrap" style="padding: 0 16rpx 8rpx" wx:if="{{memberValueGift&&orderType==5}}">
        <view class="info-title" style="padding: 24rpx">赠送内容</view>
        <equity-value-card equityList="{{storeValueEquityList}}" hasShowOneLine="{{storeValueEquityList.length===1}}" isFrom="orderDetail" wx:if="{{storeValueEquityList.length}}"></equity-value-card>
    </view>
    <view bindtap="storeValueDetail" class="store-value-info-wrapper" data-orderno="{{depositOrderAbout.orderNo}}" wx:if="{{depositOrderAbout}}">
        <view class="store-value-img">
            <image class="store-value-image" lazyLoad="{{true}}" src="{{__oss__.s(mpxExt,depositOrderAbout.itemImage,0)}}"></image>
        </view>
        <view class="store-value-info">
            <view class="store-value-item">
                <view>{{depositOrderAbout.itemName}}</view>
                <view class="strong-font">{{currencySymbol+depositOrderAbout.amount}}</view>
            </view>
            <view class="store-value-item-right">
                <view class="store-value-item-btn">查看详情</view>
            </view>
        </view>
    </view>
    <view class="info-wrap wrap" wx:if="{{!paymentOnBehalf||item.title=='配送信息'}}" wx:for="{{infoArr}}" wx:key="index">
        <view class="info-title line-bot">{{item.title}}</view>
        <view class="info-item flex" wx:for="{{item.info}}" wx:for-item="info" wx:key="index">
            <block wx:if="{{info.title==='预计送达'}}">
                <view class="info-i-label" wx:if="{{deliveryTimeShow}}">{{info.title}}</view>
                <view class="info-i-main flex items-center" wx:if="{{deliveryTimeShow}}">
                    <view class="text-right_i_">{{info.value}}</view>
                </view>
            </block>
            <block wx:else>
                <view class="info-i-label">{{info.title}}</view>
                <view class="info-i-main flex items-center">
                    <view bindtap="showBottomFrame" class="info-btn-call flex items-center" wx:if="{{info.type=='call'}}">
                        <view class="info-btn-call-line"></view>
                        <view class="iconfont i-dianhua1" style="color: {{colorTheme}};"></view>
                        <view style="color: {{colorTheme}};">{{_i40}}</view>
                    </view>
                    <view bindtap="callPhoneThirdDelivery" class="info-btn-call flex items-center" wx:if="{{info.type=='callCarrier'}}">
                        <view class="info-btn-call-line"></view>
                        <view class="iconfont i-dianhua1" style="color: {{colorTheme}};"></view>
                        <view style="color: {{colorTheme}};">{{_i41}}</view>
                    </view>
                    <view bindtap="copyText" class="info-btn-copy text-center_i_" data-value="{{info.value}}" wx:elif="{{info.type=='copy'}}">{{_i42}}</view>
                    <view bindtap="labelPreview" class="info-btn-copy text-center_i_" data-value="{{info.value}}" wx:elif="{{info.type=='labelPreview'}}">预览</view>
                    <view class="text-right_i_ line-clamp-2">{{info.value}}</view>
                </view>
            </block>
        </view>
    </view>
    <view class="info-wrap-box">
        <view bindtap="showBrandBottomFrame" class="info-wrap wrap" wx:if="{{serviceTel&&isNotStoreOrder}}">
            <view class="info-call-phone" style="color: {{colorTheme}}">
                <view class="iconfont i-dianhua1" style="margin-right: 16rpx"></view>
                <view>{{_i43}}</view>
            </view>
        </view>
        <view bindtap="toListInstore" class="info-wrap wrap" style="background-color: {{colorTheme}};color: #fff" wx:if="{{orderInfoStatus==50&&orderType==13}}">
            <view class="info-call-phone">
                <view>{{_i44}}</view>
            </view>
        </view>
    </view>
    <view class="payment-on-behalf-tip-wrapper" wx:if="{{paymentOnBehalf}}">
        <view class="payment-on-behalf-tip-title">{{_i45}}</view>
        <block wx:if="{{result.replacePaymentTips&&result.replacePaymentTips.length}}">
            <view class="payment-on-behalf-tip-text" wx:for="{{result.replacePaymentTips}}">{{index+1}}、{{item}}</view>
        </block>
        <block wx:else>
            <view class="payment-on-behalf-tip-text">1、付款前请先与好友确认无误，避免受骗</view>
            <view class="payment-on-behalf-tip-text">2、付款成功后，如需申请退款，可联系好友申请退款</view>
            <view class="payment-on-behalf-tip-text">3、当代付订单退款成功后，实付金额将原路退还代付人</view>
        </block>
    </view>
    <view class="user-refund-call-tip" style="{{userRefundCallTipBottom}}">
        <text>{{_i46}}</text>
        <text bindtap="showBottomFrame" class="call-text">{{_i47}}</text>
    </view>
</view>
<qm-loading bindreload="reload" loadErrorInfo="{{loadErrorInfo}}" loadStatus="{{loadStatus}}" wx:if="{{(!isLoad||isLoading)&&!skeletonEnableShow}}"></qm-loading>
<view class="skeleton-screen-view" style="height: {{windowHeight+'px'}};width:{{windowWidth+'px'}};" wx:if="{{(!isLoad||isLoading)&&skeletonEnableShow}}">
    <image src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2024/04/02/orderdetail-gjp.gif',0)}}" style="width: 100%; height: 100%"></image>
</view>
<v-pay-choose-select bindpaySelected="paySelected" id="payChooseSelect"></v-pay-choose-select>
<view bindtap="closeOrderAfterAdvert" class="order-after-advert-wrapper" wx:if="{{orderAfterAdvertShow}}">
    <view class="order-after-advert-main">
        <image class="order-after-advert-close" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/s16/images/2018/05/25/519d27208a811784.png',0)}}"></image>
        <image catchtap="orderAfterAdvertRouter" class="order-after-advert-img" lazyLoad="true" mode="widthFix" src="{{__oss__.s(mpxExt,orderAfterAdvertImg,0)}}"></image>
        <view class="order-after-advert-text" wx:if="{{orderAfterAdvertText}}">
            <text class="order-after-advert-text_font">{{orderAfterAdvertText}}</text>
        </view>
    </view>
</view>
<v-component-coupon id="coupon"></v-component-coupon>
<view catchtap="closeMess" catchtouchmove="true" class="rule-mask" wx:if="{{isShowGroupMessageBox}}"></view>
<view catchtouchmove="true" class="group-message-box" wx:if="{{isShowGroupMessageBox}}">
    <view catchtap="closeMess" class="close"></view>
    <view class="info-title">申请</view>
    <view class="info-text">发起微信群收款需要授权以下信息</view>
    <view class="info-ul">
        <view class="info-li">
            <icon class="icon-box-img" size="11" type="success"></icon>拼单成员的微信用户信息</view>
        <view class="info-li">
            <icon class="icon-box-img" size="11" type="success"></icon>账单信息</view>
    </view>
    <view catchtap="goGroupPay" class="info-pay-button">同意并发起群收款</view>
</view>
<view class="h5-invoice-shadow" wx:if="{{h5InvoiceUrl}}">
    <view class="h5-invoice-popup">
        <view class="h5-invoice-popup-title">复制下载链接</view>
        <view class="h5-invoice-popup-link">{{h5InvoiceUrl}}</view>
        <view class="h5-invoice-popup-note">链接复制成功后，将链接放入浏览器完成下载</view>
        <view class="h5-invoice-popup-btn-box">
            <view bindtap="cancelH5Invoice" class="h5-invoice-popup-btn" style="border-color: {{colorTheme}}; color: {{colorTheme}}">取消</view>
            <view bindtap="copyH5Invoice" class="h5-invoice-popup-btn" style="border-color: {{colorTheme}}; background: {{colorTheme}}; color: #fff">复制</view>
        </view>
    </view>
</view>
<popup-security bindclose="onHidePopupSecurity" payInfo="{{overTimePayInfo}}" show="{{showPopupSecurity}}"></popup-security>
<v-call-bottom callData="{{callData}}" id="callBottom"></v-call-bottom>
<v-brand-call-bottom id="callBrandBottom" serviceTel="{{serviceTel}}"></v-brand-call-bottom>
<view class="fixed bottom-0 z-100 flex justify-center pb-18 w-100_p_" style="{{feedbackDomBottom}}" wx:if="{{isShowFeedback&&feedbackConfig.isShow}}">
    <view class="flex justify-between items-center w-702 h-88 c-_h_fff rounded-16 shadow-_bl_0_6px_15px_rgba_pl_0_2c_0_2c_0_2c_0_d_1_pr__br_ p-x-24 box-border opacity-100" style="background-color:{{feedBackgroundColor}}">
        <view class="flex items-center">
            <view class="i-fankui text-40 mr-10" style="color:{{colorTheme}}"></view>
            <text class="c-_h_333 text-30 fw-500">{{_i48}}</text>
        </view>
        <view class="flex justify-end items-center">
            <view bindtap="goFeedback" class="w-128 h-56 c-_h_fff flex justify-center items-center rounded-120 mr-10 text-26" style="background-color:{{colorTheme}}">{{_i49}}</view>
            <view bindtap="closeFeedback" class="w-48 h-100_p_ flex justify-end items-center">
                <text class="iconfont i-close text-40 c-_h_999"></text>
            </view>
        </view>
    </view>
</view>
<v-index-ad advertInfo="{{advertInfo}}" advertInfoArr="{{advertInfoArr}}" bindjumpPage="jumpPage" bindr="__refEmit" class="ref_indexAdRef_1" data-r=".ref_indexAdRef_1" id="indexAd" pageType="orderDetail"></v-index-ad>
<blindbox-popup bindclose="hasClosedBlindBoxPopup" bindgoJump="goBlindBoxJump" bindpreview="previewMyReward" id="blindbox-popup" info="{{blindBoxRewardInfo}}"></blindbox-popup>
<blind-box-goods-detail-dialog blindBoxGoodsMulti="{{blindBoxGoodsMulti}}" id="blindBoxGoodsDetailDialog"></blind-box-goods-detail-dialog>
<view class="safe-shadow" wx:if="{{isLock}}"></view>
<view class="order-friendly-tips" wx:if="{{isShowFriendlyTips&&!paymentOnBehalf&&!isBindOther}}">
    <view class="order-friendly-tips__main">
        <view class="order-friendly-tips__title">
            <image class="order-friendly-tips__icon" lazyLoad="true" src="{{__oss__.s(mpxExt,'https://images.qmai.cn/resource/20210824210816/2021/12/28/order-friendly-tips-icon1.png',0)}}"></image>
            <view class="order-friendly-tips__note">温馨提示</view>
        </view>
        <view class="order-friendly-tips__content" wx:for="{{orderFriendlyTips}}">{{index+1}}. {{item}}</view>
        <view catchtap="closeFriendlyTips" class="order-friendly-tips__close flex items-center justify-center">
            <view class="i-guanbi2 text-_bl__h_bfbfbf_br_ text-24"></view>
        </view>
    </view>
</view>
<books-integral-popup bindclose="closeIntegralPop" booksAmount="{{booksAmount}}" effectImage="{{effectImage}}" effectSound="{{effectSound}}" id="booksIntegralPop" show="{{isShowBooksPop}}"></books-integral-popup>
<advert-suspension-window advertImgSrc="{{levitatedSphereAdverObj.imgSrc}}" advertInfo="{{levitatedSphereAdverInfo}}" id="advertSuspensionWindow" isShowAdvertFlag="{{levitatedSphereAdverObj.isShowAdvertFlag}}" location="left" pageType="orderDetail" wx:if="{{isShowLevitatedSphereAdver}}"></advert-suspension-window>
<integral-exchange-popup bind:close="onClose" integralList="{{pointComingBenefitInfo&&pointComingBenefitInfo.mallGoodsList}}" userPoints="{{pointComingBenefitInfo&&pointComingBenefitInfo.currentPoint}}" visible="{{integralExchangePopupvisible}}"></integral-exchange-popup>
<authorization bindcomplete="authorize" force="{{true}}" id="authorization"></authorization>
<v-pay-choose bindpayFail="payForAnotherFail" bindpaySuccess="payForAnotherSuccess" bindr="__refEmit" class="ref_payChoose_2" currencySymbol="{{currencySymbol}}" data-r=".ref_payChoose_2" disableDepositPay="{{mutexDepositPay}}" from="payForAnother" outOrderNo="{{orderNo}}" typeCate="{{orderType}}" wx:if="{{paymentOnBehalfUserId}}"></v-pay-choose>
<block wx:if="{{showVirtualRmb}}">
    <v-pay-choose noBalancePay bindpaySuccess="paySuccess" bindpaymentWaitChange="paymentWaitChange" bindr="__refEmit" class="ref_continuePayChoose_3" currencySymbol="{{currencySymbol}}" data-r=".ref_continuePayChoose_3" disableDepositPay="{{mutexDepositPay}}" from="continuePay" orderSubType="{{result.orderSubType}}" outOrderNo="{{orderNo}}" payScene="{{1}}" typeCate="{{orderType}}"></v-pay-choose>
    <payment-wait bindpayFail="continuePayFail" bindpaySuccess="continuePaySuccess" bindr="__refEmit" class="ref_paymentWaitRef_4" data-r=".ref_paymentWaitRef_4"></payment-wait>
</block>
<std-advertisement id="std-advertisement" position="5"></std-advertisement>
<view wx:if="{{isFromPOSScanCode&&showBindPopup}}">
    <view bindtap="closeBindPopup" class="subscribe-dialog-mask"></view>
    <view class="subscribe-dialog-content">
        <view class="subscribe-dialog-title">温馨提示</view>
        <view class="subscribe-dialog-tip">您当前未登录，可能无法获取订单积分等信息</view>
        <view class="subscribe-dialog-btns">
            <view bindtap="closeBindPopup" class="subscribe-dialog-btn-cancel">取消</view>
            <view catchtap="runAutoMobile" class="subscribe-dialog-btn-confirm" wx:if="{{!isAuth}}">立即登录</view>
            <view catchtap="invoicingDetailBindOrderUser" class="subscribe-dialog-btn-confirm" wx:elif="{{needBind}}">立即绑定</view>
        </view>
    </view>
</view>
<view wx:if="{{isFromPOSScanCode&&subscribeDialogShow&&!isBindOther}}">
    <view bindtap="hideSubscribe" class="subscribe-dialog-mask"></view>
    <view class="subscribe-dialog-content">
        <view class="subscribe-dialog-title">温馨提示</view>
        <view class="subscribe-dialog-tip">订阅后，可立即接收取餐提醒</view>
        <view class="subscribe-dialog-btns">
            <view bindtap="hideSubscribe" class="subscribe-dialog-btn-cancel">取消</view>
            <view bindtap="subscribeMessagePOS" class="subscribe-dialog-btn-confirm">立即订阅</view>
        </view>
    </view>
</view>
<subscribe-mask show="{{isSubscribeMaskShow}}"></subscribe-mask>
<wechat-state-bubble isPayed="{{isPayed}}" lunckyStateBgImg="{{lunckyStateBgImg}}" lunckyText="{{kindlyPasscodeText}}" orderInfo="{{result}}" page="orderDetail" payType="{{result.orderInfo.payType}}" zIndex="1" wx:if="{{'wx'==='wx'&&isLoad&&isPayed==1}}"></wechat-state-bubble>
<std-popup bindclose="closeStickImagePopup" customStyle="background-color: transparent" show="{{stickImagePopupShow}}" wx:if="{{result.cupStick}}">
    <view class="flex flex-col items-center">
        <image class="w-480rpx h-650rpx border-6rpx border-hex-ffffff border-solid mt-24rpx bg-white" mode="aspectFill" src="{{__oss__.s(mpxExt,result.cupStick.image,0)}}"></image>
        <view catchtap="closeStickImagePopup" class="iconfont i-chahao text-60rpx text-white mt-30rpx"></view>
    </view>
</std-popup>
<luncky-share-poster bindclose="hideSharePoster" lunckySwiperBannerInfo="{{activityDiyConfigBySellerMap[15]&&activityDiyConfigBySellerMap[15].bannerInfo}}" lunckyText="{{kindlyPasscodeText}}" show="{{showLuckySharePoster}}" wx:if="{{showLuckySharePoster}}"></luncky-share-poster>
<v-call-confirm id="vCallConfirm"></v-call-confirm>
<std-popup bindclose="closeSurveyPop" customStyle="background-color: transparent;" show="{{isShowSurveyPopup}}" zIndex="10002">
    <view class="relative box-border w-600rpx h-900rpx">
        <view class="w-600 h-800 border-rd-24rpx">
            <image catchtap="startSurveyBanner" class="w-600 h-800 border-rd-24rpx" mode="aspectFill" src="{{__oss__.s(mpxExt,orderDetailPopImg,0)}}"></image>
        </view>
        <view class="absolute bottom-0 left-50_p_ ml--40rpx flex items-center justify-center w-80 h-80">
            <text catchtap="closeSurveyPop" class="i-guanbi4 text-white text-60 opacity-50"></text>
        </view>
    </view>
</std-popup>
<wxs module="__oss__" src="../../wxs/oss28ac43a0.wxs"/>
<wxs module="tools" src="wxs/tools22b08362.wxs"/>